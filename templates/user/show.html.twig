{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class="bg-gradient">
    <div class="uk-card uk-position-center uk-card-default uk-card-body uk-width-1-2@m">
        <h1>Mon profil</h1>

        <table>
            <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ user.nom }}</td>
                </tr>
                <tr>
                    <th>Prénom</th>
                    <td>{{ user.prenom }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ user.email }}</td>
                </tr>
            </tbody>
        </table>
        <div class="btn-rc"></div>
        {% if is_granted("ROLE_ADMIN") %}
        <a class="btn-blue bg-blue" href="{{ path('app_user_index') }}">Utilisateurs</a>

        <a class="btn-blue bg-blue" href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>
        <a class="btn-red style-unset">Supprimer{{ include('user/_delete_form.html.twig') }}</a>
        {% endif %}

    </div>
</div>

{% endblock %}
