{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="bg-gradient">
        <div class="uk-position-center uk-card uk-card-body uk-card-default">
            <h1>Edit User</h1>
                <div class="btn-rc">
                    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
            <div class="btn-rc">
            <a class="btn-blue" href="{{ path('app_user_index') }}">back to list</a>
            </div>

        </div>
    </div>
{% endblock %}
